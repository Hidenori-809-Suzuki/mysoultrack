# docker-compose.yml が読み込む環境変数。
# コピーして .env を作成し、実際の値を設定してください。
#   cp .env.example .env
# .env はコミットしないこと（.gitignore 対象）。

MYSQL_ROOT_PASSWORD=changeme
MYSQL_DATABASE=soultrack
MYSQL_USER=soultrack
MYSQL_PASSWORD=changeme
